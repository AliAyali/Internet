package com.aliayali.internet.core

/**
 * Enum representing the current connection state during network testing.
 */
enum class ConnectionState {
    Idle,
    Scanning,
    Connected,
    Disconnected
}